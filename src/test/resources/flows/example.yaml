id: example
namespace: io.kestra.zapier

tasks:
- id: date
  type: io.kestra.plugin.zapier.Example
  format: "{{taskrun.startDate}}"
- id: task-id
  type: io.kestra.plugin.zapier.Example
  format: "{{task.id}}"
- id: flow-id
  type: io.kestra.plugin.zapier.Example
  format: "{{outputs['task-id'].child.value}}"
